# Automatic Oracle Cloud Ubuntu VMs basic config

How to use the script:

```
$ wget -O - https://raw.githubusercontent.com/grmelacz/oracle-cloud-init/refs/heads/main/oracle-init.sh | bash
```

The script does use use non-interactive commands (e.g. for updating packages), however you'll still need to enter your password when asked as most of the commands require root access.

